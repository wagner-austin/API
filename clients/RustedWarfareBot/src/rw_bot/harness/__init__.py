"""Operating the game process: launch configuration and boot-log decoding.

This package owns everything about running the engine unattended. It does not
decide anything about gameplay — that belongs to the planner.
"""

from __future__ import annotations
