# Railway configuration for covenant-radar-api

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
dockerfileTarget = "api"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
