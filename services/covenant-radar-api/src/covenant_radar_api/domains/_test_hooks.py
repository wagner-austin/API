"""Test hooks for domains base abstraction layer.

The base schemas, protocols, and registry are pure in-memory constructs
with no external dependencies. No hookable dependencies exist.
"""

from __future__ import annotations

__all__: list[str] = []
