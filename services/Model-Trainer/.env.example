# Minimal environment overrides (optional)
# Redis URL (override if using external Redis)
REDIS_URL=redis://platform-redis:6379/0

# Optional: custom config file path (TOML)
# APP_CONFIG_FILE=./server/config/app.toml

# Optional: override PyTorch wheel index for GPU builds
# Default is set by docker-compose to CUDA 12.4 wheels; you can pin here instead
# TORCH_INDEX=https://download.pytorch.org/whl/cu124
