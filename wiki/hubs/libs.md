# Libs

Shared libraries in `libs/` — the reusable core that services and clients build on. Search here BEFORE adding a cross-cutting helper. Two families:

- **platform_*** — infrastructure primitives: `platform_core` (config, logging, HTTP, FastAPI utils, OAuth 2.0 + PKCE), `platform_workers` (RQ + Redis), `platform_ml` (ML artifact storage, device auto-detection), `platform_discord`, `platform_music`, `platform_email` (Outlook Graph + Gmail), `platform_calendar` (Google Calendar), `platform_codebase`, `platform_devpost` (Devpost hackathon discovery + codebase capability matching), `platform_kaggle`, `platform_stt` (Whisper), `platform_langid` (Meta MMS-LID), `platform_translate` (Anthropic + OpenAI backends).
- **domain libs** — narrower vertical stacks: `covenant_domain` / `covenant_ml` / `covenant_nn` / `covenant_persistence` (loan-covenant modeling), `cleargbm` + `cleargbm_rs` (interpretable gradient boosting, numpy + Rust), `procart` (procedural art / neon HDR).
- **instrument_io** — scientific instrument format readers (mass spec, mzML, Excel, PDF).
- **monorepo_guards** — the code-quality rule engine (20+ static analysis checks, Python + Rust).

[platform_workers — RQ + Redis pattern](../pages/platform-workers-rq-pattern.md) -- typed Redis client factories (`redis_for_kv` / `redis_for_rq` / `redis_raw_for_rq` / `redis_for_pubsub`) + RQ harness (`rq_queue`, `rq_retry`) + `readyz_redis` health helper
[ClearGBM histogram-based split finding](../pages/cleargbm-histogram-split-path.md) -- O(K) prefix-sum bin scan, sibling subtraction, reg_lambda semantics, Rust-only compute path
[ClearGBM Python-Rust boundary after the shim removal](../pages/cleargbm-python-rust-boundary.md) -- maturin builds cleargbm_rs top-level with a generated wrapper (ea7835d2); _rust.py Protocol layer is the only Python mirroring Rust signatures; ensemble.py = validate + marshal + one call; ImportError with no fallback; dependency points Python -> Rust only
[ClearGBM perf — column-major sample_bins layout](../pages/cleargbm-perf-column-major-sample-bins.md) -- transpose FeatureBins.sample_bins from row-major Vec<Vec<usize>> to column-major flat Vec<usize>; contiguous per-feature scans, 20-40% faster fit (highest ROI perf change)
[ClearGBM perf — uint8 histogram bin dtype](../pages/cleargbm-perf-uint8-histogram-bins.md) -- bin index dtype usize -> u8, cap max_bins <= 255, 8x more values per cache line, 30-60% faster fit; land alongside column-major refactor
[ClearGBM perf — SIMD histogram accumulator](../pages/cleargbm-perf-simd-histogram-accumulator.md) -- the last unshipped roadmap item, re-based 2026-07-30: Approach 1 already shipped as the hand-written 8-wide unroll, only bin-first reordering remains, and the residual 1.426× gap is tree-shape not histogram throughput — recommendation is now NOT to start it
[ClearGBM perf — leaf-wise tree growth](../pages/cleargbm-perf-leaf-wise-growth.md) -- replace depth-first LIFO stack with best-first max-heap over split gains; capacity gain, not speed; do LAST — trades interpretability for capacity
[ClearGBM perf experiments — 2026-07-21 session](../pages/cleargbm-perf-experiments-2026-07-21.md) -- empirical log: 4.3× shipped (6.88s → 1.60s), 3 negative-result experiments recorded so future sessions don't repeat (ordered arrays unpooled −4%, counts-backfill −5%, cnt_factor reconstruction −16% + quality drift); its Phase-I gap figure is superseded — see the leaf-normalized page
[ClearGBM — f32 score narrowing, shipped then reverted](../pages/cleargbm-f32-score-narrowing-reverted.md) -- the LightGBM `score_t = float` shape landed 2026-07-21 and was removed 2026-07-25; why the "+15%" and the "8% slower" both stand, and why the 8% figure has no benchmark artifact
[ClearGBM program charter](../pages/cleargbm-program-charter.md) -- the operator's 2026-08-22 mandate: AI-first tabular learning across covenant/weather/RustedWarfare/metabolomics domains, the four-rule constitution (config honesty, per-config determinism, safe Rust, measured-or-nothing), and the six-phase roadmap from regression to HPC3-scale experimentation
[ClearGBM objective seam (P1)](../pages/cleargbm-objective-seam.md) -- squared-error regression behind one Objective enum, landed 2026-08-22: required objective field, objective-paired scale_pos_weight, typed labels; binary path proven byte-identical (56/56 manifest values), and ClearGBM leads its first regression benchmark (financial_distress RMSE 1.8023 vs xgboost 1.8291, lightgbm 1.8637) at the fastest wall clock
[ClearGBM sample weights (P2)](../pages/cleargbm-sample-weights.md) -- per-row weights on both objectives, landed 2026-08-23: weights are data not config, None/all-ones/spw-derivation all proven bit-identical (56/56 manifest values reproduced), zero weights rejected; the substrate GOSS and LambdaMART consume
[ClearGBM min_data_in_bin](../pages/cleargbm-min-data-in-bin.md) -- landed 2026-08-25: the coarseness dial the binning re-baseline named (config field 25, required-with-null, Some(1) rejected as an alias of null); conventional floors are inert at rw_matches scale, but floor 32768 recovers the retired regularization — 0.7460 held-out AUC vs LightGBM's 0.7299 on the identical protocol, a real optimum (65536 declines); knob-sensitivity detectors at three layers, one of which caught a briefly-planted decorative None before any gate
[ClearGBM count-aware binning](../pages/cleargbm-count-aware-binning.md) -- landed 2026-08-25: the quantile-of-multiset edge rule starved skewed features (weather's hot_excess: 2,359 distinct values → 6 bins; voc species merged) and was replaced with LightGBM's shipped GreedyFindBin semantics on (value, count) pairs, no new knob; both adverse corpora flip to ClearGBM leads (weather 2.0681 vs 2.0695; voc 18.173 vs 18.317), rw_value improves, us-binary leaf-wise ties LightGBM's AUC exactly; mechanism pinned by a bin-count probe + a 48-member HPC3 protocol-grid rung under old binning
[ClearGBM on HPC3 + four new-domain corpora (P6 Landings A+B1-B4)](../pages/cleargbm-hpc3-farm-and-rw-value.md) -- landed 2026-08-24: the experiment farm runs (16-member sweep, 320 trials, 0 failures; per-job output filenames fix the fan-out's shared-writer hazards); rw_value (569k rows, 99 match groups) joins with ClearGBM leading mean RMSE; weather_tmax (43k GHCN-D summer rows, 15 stations, day-ahead anomaly through the deployed extractor) joins honestly adverse, LightGBM leading; metab_confidence (17.6k SIRIUS rank-1 structure calls, pre-annotation predictors only) joins as a weak-signal entry — four arms tied, R² 0.004-0.025; voc_match_quality (5,850 GC-MS peaks across 10 field sites, NIST match quality from species + RT + chromatogram context via a new stdlib XLSX reader) joins with real signal (R² 0.37-0.48), LightGBM leading and ClearGBM second at 0.7%
[ClearGBM quantized training (P5 Landing C)](../pages/cleargbm-quantized-training.md) -- quantized_gradient_bins (config field 24), landed 2026-08-23: Shi 2022 as LightGBM ships it — int8 stochastic rounding, per-node 16/32-bit packed integer histograms, exact integer scan rescaled at the boundary; split training exact under quantization; measured honestly: +0.0029 AUC (regularization) at ~1.1-1.5x wall clock single-threaded, the paper's 2x refuted at our scales for both libraries
[ClearGBM continued training + GOSS (P5 Landings A+B)](../pages/cleargbm-continuation-and-goss.md) -- landed 2026-08-23: continuation appends trees to a self-contained artifact under the model's own config (split training exact to the bit; inverts LightGBM's delta-model shape); GOSS as a required-with-null rate pair with shipped-code semantics (|g x h| rank, 1/lr warmup, both-stream reweighting), quality cost matching LightGBM's own (-0.0072 vs -0.0073 AUC); identity 112/112 twice, artifacts retrained round 5
[ClearGBM LambdaMART ranking (P4 Landing B)](../pages/cleargbm-lambdarank.md) -- pair lambdas by query on the objective seam, landed 2026-08-23: groups are data via a dedicated entry, lambdarank_truncation_level required-with-null (config field 21), exact sigmoid + always-on lambda norm (documented divergences), counting-sort max DCG, ES on 1-NDCG; NDCG@10 parity with LGBMRanker, identity 112/112
[ClearGBM multiclass softmax (P4 Landing A)](../pages/cleargbm-multiclass-softmax.md) -- K trees per round on the objective seam, landed 2026-08-23: class-major buffers, uncentered log-prior bases, K/(K-1) hessian rescale, whole-round early stopping, prediction trio; n_classes required-with-null (config field 20) + class_base_predictions (model field 6); identity 112/112, log loss below LightGBM on all four quality seeds
[ClearGBM categorical splits (P3 Landing B)](../pages/cleargbm-categorical-splits.md) -- many-vs-many set-membership splits over integer codes, landed 2026-08-23: gradient-sorted prefix scan (Fisher order), no silent cat_smooth or rare-category grouping, SHAP decoder refuses categorical models; identity 112/112, artifacts retrained exactly
[ClearGBM NaN direction + colsample_bytree (P3 Landing A)](../pages/cleargbm-nan-direction-and-colsample.md) -- missing-value routing found ALREADY learned per split (stump-discriminator test pins the flip; the spec's fixed-policy premise was wrong); colsample_bytree lands as a required-with-null (0,1)-exclusive per-round mask on its own RNG stream, max_features drawing within it; identity 112/112, five artifacts retrained exactly
[ClearGBM — the decorative-knob class](../pages/cleargbm-decorative-knob-class.md) -- five config fields that never reached training (reg hardcodes, phantom scale_pos_weight, dropped max_features, unimplementable track_contributions, the sweep-save crash), all fixed/removed 2026-08-22; +1.5 AUC pts recovered on rw_matches, the us "0.87" exposed as a model production never trained, and the knob-sensitivity detector that catches the class
[ClearGBM vs LightGBM — leaf-normalized benchmarking](../pages/cleargbm-leaf-normalized-benchmarking.md) -- why a wall-clock ratio between a depth-wise and a leaf-wise learner is not an implementation verdict; at depth 6 cleargbm builds 1.87× the leaves, and is 0.937× per leaf (i.e. ~6% faster at equal work); the measurement protocol and where the harness lives

<!-- Add pages here as they're written. Format: [Title](../pages/<slug>.md) -- one-line description -->
