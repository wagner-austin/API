"""What the monorepo's Makefile recipes call instead of a shell.

Standard library only, launched by path with the system interpreter from
``scripts/run.py``; see ``README.md`` for the commands and the grammar.
"""
